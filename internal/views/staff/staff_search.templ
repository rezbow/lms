package members

import (
	"lms/internal/models"
	"lms/internal/views/common"
)

templ StaffSearch(staff []models.Staff, data *models.SearchData) {
	<div>
		<form
			role="search"
			method="GET"
			action="/staff/search"
		>
			<input name="q" type="search" placeholder="جستوجو" value={ data.Term }/>
			<input type="submit" value="جستوجو"/>
		</form>
		<div id="results">
			@StaffList(staff, data)
		</div>
		@common.Pagination(data)
	</div>
}
